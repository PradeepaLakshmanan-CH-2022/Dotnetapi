version: 0.0
os: windows
files:
  - source: /index.html
    destination: /var/www/html/
hooks:
  ApplicationStart:
    - location: scripts/before-install.bat
      timeout: 300
      runas: Administrator
